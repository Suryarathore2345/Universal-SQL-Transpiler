# Pinned to the exact versions this codebase is tested against (verified
# via the full pytest suite — see backend/tests/). Previously this file
# used unbounded lower-bound-only ranges (e.g. `sqlglot>=25.0.0`), so a
# fresh `pip install` could silently pull a newer major version with
# behavior changes the code was never tested against — sqlglot in
# particular is core to every dialect parser/generator.
#
# To upgrade a dependency deliberately: bump the version below, run the
# full test suite, and fix or accept any resulting diffs (including
# regenerating golden snapshots with `pytest tests/test_phase6_golden.py
# --regen-golden` if the change is an intentional behavior improvement)
# before committing the bump.

# --- Direct dependencies ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
sqlglot==30.11.0
PyYAML==6.0.3
python-multipart==0.0.32

# --- Transitive dependencies (pinned for reproducible installs) ---
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.5.20
click==8.4.1
colorama==0.4.6
h11==0.16.0
httptools==0.8.0
idna==3.18
pydantic_core==2.46.4
Pygments==2.20.0
python-dotenv==1.2.2
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
watchfiles==1.2.0
websockets==16.0
